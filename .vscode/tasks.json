{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "C/C++: compilación activa",
            "type": "shell",
            "command": "g++",
            "args": [
                "-g", // Generate debugging information
                "${file}", // The currently active source file
                "-o", // Specify the output file
                "${fileDirname}/${fileBasenameNoExtension}" // Output path and filename
            ],
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "problemMatcher": [
                "$gcc"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "detail": "Task generated by Debugger."
        }
    ]
}
